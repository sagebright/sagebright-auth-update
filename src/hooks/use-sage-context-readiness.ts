
import { useSageContextReadiness } from './sage-context';

/**
 * Re-export the useSageContextReadiness hook to maintain imports compatibility
 * with existing code that uses this file path
 */
export type { SageContextReadiness } from './sage-context';
export { useSageContextReadiness };
