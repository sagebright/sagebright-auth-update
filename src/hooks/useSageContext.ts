
import { useSageContext } from './sage-context/use-sage-context';

/**
 * Re-export the primary useSageContext hook to maintain imports compatibility
 * with existing code that uses this file path
 */
export { useSageContext };
export default useSageContext;
